import useColorScheme from '@/hooks/useColorScheme';
import { NavigationConfig } from '@rnstudy/react-native-navigation';

export const config: NavigationConfig = {
  useColorScheme,
};

export default config;
