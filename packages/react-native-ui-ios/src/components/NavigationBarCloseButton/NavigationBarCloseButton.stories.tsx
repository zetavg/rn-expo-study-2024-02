import type { Meta } from '@rnstudy/storybook-rn-types';

import NavigationBarCloseButton from './NavigationBarCloseButton';

const meta: Meta<typeof NavigationBarCloseButton> = {
  title: 'iOS UI/NavigationBarCloseButton',
  component: NavigationBarCloseButton,
};

export default meta;

export const Default: Meta<typeof NavigationBarCloseButton> = {};
