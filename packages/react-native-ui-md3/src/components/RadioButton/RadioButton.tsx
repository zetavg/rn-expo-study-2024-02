import {
  RadioButton as RNRadioButton,
  type RadioButtonProps as RNRadioButtonProps,
} from 'react-native-paper';

export type Props = RNRadioButtonProps;

export const RadioButton = RNRadioButton;

export default RadioButton;
